# Artefactos de herramientas locales
.gstack/
.DS_Store

# Dependencias y build (desde la semana 1)
node_modules/
.next/
dist/

# Secretos — ninguna llave vive en el repo
.env
.env.*
!.env.example

# TypeScript / vitest
coverage/
*.tsbuildinfo

# Next.js
.next/
next-env.d.ts
# Vercel
# El `.env*` que agrega `vercel link` se quitó a propósito: es más amplio que
# las reglas de arriba y va DESPUÉS del `!.env.example`, así que volvería a
# ignorar la plantilla que sí se versiona.
.vercel
